Command:  
```ps1
git remote -v
```

Return:  
```ps1
origin  https://github.com/mortyhawky/win_install.git (fetch)
origin  https://github.com/mortyhawky/win_install.git (push)
```

```ps1
git status
git add -Av
git commit -m "$(date)"
git push
```
